<?xml version="1.0" encoding="UTF-8"?>

<!-- ==== mapper 기본설정 ==== -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- ==== 루트 엘리먼트 & 네임스페이스 설정(프로젝트 전체내에서 유일해야 한다.) ==== -->

<mapper namespace="comment">
	
	<!-- 댓글 목록 -->
	<select id="getCommentList" parameterType="HashMap" resultType="CommentDTO">
		select commentNo, fk_id, fk_name, content, createdAtComment, updateAtComment, fk_boardNo, parentNo
		from comments
		where fk_boardNo = #{boardNo}
	</select>
	
	
	<!-- 댓글 쓰기 -->
	<insert id="commentWrite" parameterType="CommentDTO">
		insert comments(commentNo, fk_id, fk_name, content, createAtComment, updateAtComment, fk_boardNo)
		values(#{comment_seq}, #{fk_id}, #{fk_name}, #{content}, default, default, #{fk_boardNo})
	</insert>
	
	
	<!-- === users 테이블의 point 컬럼의 값을 50점을 증가(update) === -->
	<update id="updateUsersPoint" parameterType="HashMap">
		update users set point = point + to_number(#{point})
		where id = #{id}
	</update>

</mapper>





